#include <iostream>
using namespace std;
struct LinkedList
{
    LinkedList *next;
    LinkedList *prev;
    int data;   
};
void display(LinkedList *head)
{
    LinkedList *current=head;
    while(current->next!=head)
    {
        
    }
}

